yum clean all

#  launch elastic search service node
docker run --name es01 -it -m 1GB docker.elastic.co/elasticsearch/elasticsearch:9.2.1

# open new terminal
docker ps

# at client side
docker inspect es01
export ELASTIC_PASSWORD=sfgfhrhhjtgewf
curl -X GET -k -u elastic:$ELASTIC_PASSWORD  https://172.17.0.2:9200/

############## Above this - remove #########################


# use it to remove first, 
docker rm es01

#  launch elastic search service node
docker run --name es01 -dit -p 9200:9200 --net elastic  -m 1GB docker.elastic.co/elasticsearch/elasticsearch:9.2.1

docker logs es01

export ELASTIC_PASSWORD=<ELASTIC USER PASSWORD>
curl -X GET -k -u elastic:$ELASTIC_PASSWORD  https://localhost:9200/

# Getting  token
docker logs es01
or
docker exec -it es01 /usr/share/elasticsearch/bin/elasticsearch-create-enrollment-token -s node

export ENROLLMENT_TOKEN=<token>
docker run -e ENROLLMENT_TOKEN="<token>" --name es02 --net elastic -dit -m 1GB docker.elastic.co/elasticsearch/elasticsearch:9.2.1
curl -X GET -k -u elastic:$ELASTIC_PASSWORD  https://localhost:9200/_cat/nodes?v


docker run -e ENROLLMENT_TOKEN="<token>" --name es03 --net elastic -dit -m 1GB docker.elastic.co/elasticsearch/elasticsearch:9.2.1
docker run -e ENROLLMENT_TOKEN="<token>" --name es04 --net elastic -dit -m 1GB docker.elastic.co/elasticsearch/elasticsearch:9.2.1
curl -X GET -k -u elastic:$ELASTIC_PASSWORD  https://localhost:9200/_cat/nodes?v

curl -X GET -k -u elastic:$ELASTIC_PASSWORD  https://localhost:9200/_cat/health?v
curl -X GET -k -u elastic:$ELASTIC_PASSWORD  https://localhost:9200/_cat/master?v



docker rm kib01
docker run --name kib01 -dit --net elastic -p 5601:5601 docker.elastic.co/kibana/kibana:9.2.1

docker exec -it es01 /usr/share/elasticsearch/bin/elasticsearch-create-enrollment-token -s kibana

docker logs kib01

URL : ip address of base linux 
# ifconfig enp0s3

go windows: https://baselinuxip:5601







